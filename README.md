## DirectPipe

---

A browser-to-browser file transfer app built on WebRTC — no size limits, no uploads to a server, no accounts. Share a link, the other person opens it, and the file streams peer-to-peer from disk to disk.
